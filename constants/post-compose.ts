/** 写作页正文草稿（刷新/误关可恢复） */
export const POST_COMPOSE_DRAFT_KEY = 'blog-cms:post-compose-draft'

/** 从「上传 Markdown」进入写作页时携带的初始元数据 */
export const POST_COMPOSE_IMPORT_KEY = 'blog-cms:post-compose-import'

/** 编辑页 → 保存页：正文与元数据暂存（sessionStorage） */
export const POST_EDIT_SAVE_STASH_KEY = 'blog-cms:post-edit-save-stash'

/** 新建/导入 → 保存页：正文与元数据暂存（sessionStorage） */
export const POST_CREATE_SAVE_STASH_KEY = 'blog-cms:post-create-save-stash'
